/**
 * Point d'entrée unique des types métier.
 *
 * Ils décrivent ce que renvoie le backend Nest (voir `@/lib/api/types`), plus
 * de schéma Supabase côté webapp.
 */
export type * from '@/lib/api/types';
