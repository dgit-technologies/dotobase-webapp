# API backend Nest (dépôt dotobase-backend).
# La webapp ne parle plus à Supabase : Supabase n'est plus que la base de
# données du backend, qui seul détient la service role key.
# Local : http://localhost:4000 (doc interactive sur /docs)
NEXT_PUBLIC_API_URL=http://localhost:4000

# URL de l'app
NEXT_PUBLIC_APP_URL=http://localhost:3000
